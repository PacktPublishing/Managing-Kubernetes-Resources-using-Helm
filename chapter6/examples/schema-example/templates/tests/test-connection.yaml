apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "schema-example.fullname" . }}-test-connection"
  labels:
    {{- include "schema-example.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "schema-example.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
